import {
    matchWhen,
    matchDefault,
    matchType,
    matchBook,
    matchStrike,
    matchUnwrap,
} from './functions';

export {
    matchWhen as when,
    matchDefault as _,
    matchType as type,
    matchBook as book,
    matchUnwrap as unwrap,
    matchStrike as strike,
};
