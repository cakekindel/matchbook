import { otherwise, match, strike, pattern, unwrap } from './functions';

// prettier-ignore
export {
    match,
    match as m,
    strike,
    pattern,
    unwrap,
    otherwise,
    otherwise as _,
    otherwise as fallback,
};
