import { strike } from '../../src/functions';

describe('strike.fn', () => {
    it('should not throw', () => {
        // arrange
        // act
        // assert

        strike(undefined, {});
    });
});
